{
  "name": "uikit-launch-video",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "GSAP-animated launch teaser for uikit.studio, rendered frame-exact to MP4.",
  "scripts": {
    "data": "node build-data.mjs",
    "preview": "node serve.mjs",
    "render": "node render.mjs --format 16x9",
    "render:vertical": "node render.mjs --format 9x16",
    "render:all": "node render.mjs --format 16x9 && node render.mjs --format 9x16",
    "setup": "npm install && npx playwright install chromium && node build-data.mjs"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0",
    "gsap": "^3.12.5",
    "playwright": "^1.49.0"
  }
}
